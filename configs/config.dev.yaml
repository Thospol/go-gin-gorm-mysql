database:
  mysql:
    host: "localhost"
    port: "3306"
    username: "root"
    password: "password"
    database_name: "local"
    driver_name: "mysql"
    protocol: "tcp"
    timeout: "5s"

http_server:
  read_timeout: 10s
  write_timeout: 10s
  read_header_timeout: 10s